"use strict";

const Promise = require("bluebird");
const donut = require("..");
const _ = require("lodash");

(async () => {
  try {
  } catch (e) {
    console.error(e);
    process.exit(1);
  }
})();
